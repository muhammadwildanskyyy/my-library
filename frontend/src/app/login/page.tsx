import LoginForm from "@/components/auth/LoginForm";

export const metadata = {
  title: "Sign In — AI Librarian",
  description: "Sign in to your AI Librarian account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
