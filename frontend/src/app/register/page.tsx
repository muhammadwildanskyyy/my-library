import RegisterForm from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Register — AI Librarian",
  description: "Create your AI Librarian account.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
